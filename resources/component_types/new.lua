new = {
	-- Lifecycle function that runs at the components creation
	OnStart = function(self)

	end,

	-- Lifecycle funcion that runs every frame
	OnUpdate = function(self)

	end,

	-- Lifecycle funcion that runs at the end of every frame
	OnLateUpdate = function(self)

	end
}